package test

import (
	"testing"
)

func TestServer(t *testing.T) {
	assert.Equal(t, 1,1)
	// engine := gin.Default()
	// engine.Post("/service/register", register.Register)
	// engine.Post("/service/unregister", register.UnRegister)
	// engine.Run(":8899")

}
